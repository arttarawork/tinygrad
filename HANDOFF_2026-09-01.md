# HANDOFF_2026-09-01.md — THE entry point (supersedes HANDOFF_2026-08-31.md)

## §0 State in one paragraph
The 2026-09-01 hardware window (Artur's grant) closed the **fault era for real**: E1 read the GSP WPR2 registers and
confirmed the client allocator's top 129 MiB sat INSIDE the GSP-protected region (T4.75 hypothesis B); **T4.77**
(vram−256MB ceiling, one line) was validated by kill-test — the deterministic loaded-session fault-storm recipe
(state-cache clone pressure) went from faults=8 twice to **faults=0 OOM=0**; the storms' MemoryErrors were post-fault
echoes. **T4.78** forensics (dispatch ring + wedge short-circuit + eager drain) produced the first-ever named-dispatch
fault attribution. Both are PRs **#25/#26, 51/51 green, awaiting merge**. The WY bug (T4.73) split into TWO bugs and
remains **unfixed but fully characterized**; WY/state-cache/MTP still ship OFF. Q8 35B @192k is the standing server.

## §1 Read order
1. This file; then `TASKS.md` "RESUME HERE" + the Status log rows from 2026-09-01 (the window, top-down).
2. `T475_NV_FAULT_RCA.md` (+§8 E1 addendum) — the fault mechanism, now confirmed.
3. `FIXNOTES.md` on branch `task/T4.73-wy-replay-fix` + `REPORT.md`/`AUDIT.md` on the T4.76/T4.73 branches — the WY two-bug record.
4. `~/CLAUDE.md` "Pooled model" — the serving ritual (unchanged).

## §2 Measured truth (this window)
- E1: WPR2 = [23.812, 23.999] GiB on the 3090; old ceiling vram−64MB ⇒ 129 MiB overlap. T4.77 = vram−256MB.
- E3 recipe (16k prefill + essay + 16k-suffix + 5.9k probe, STATE_CACHE_MB=1024): storms 2/2 on master
  (faults=8 each; ring named snapshot_state's per-block clone pair in flight: `E_8192_32_3` 3MB state +
  `E_320_32_3` 60KB conv, behind 94 NV→NV copies); **faults=0 OOM=0 on T4.77**. Fresh-boot store alone never faults (0/3).
- T4.73 bug-1 (the serve killer): WY-only (loop@32 serve round clean), replay-triggered (warmup burns cnt 0+1 at the
  prefill jit key via `or out is None`; every real prefill then REPLAYS — WY_TRACE shows stale impl + NaN-flooded
  recurrent/conv states in blocks 1..62 + one head of blk0, constant `KeyError: 248320`), width-independent (chunk 32
  and 16 identical), placement-independent (METAL+NV split and all-GDN-on-METAL identical), materialization-independent
  (three .contiguous() candidates refuted), warmup-length-independent (1-tok and 3-tok dummies identical), BEAM-independent.
  **Refuses to reproduce below serve scale**: tiny real-generate on CPU AND METAL clean (instrument caveat: all-zero states
  every arm); the T4.76 standalone repro's divergence turned out to be bug-2, not this.
- T4.73 bug-2 (found while hunting bug-1): transient one-call wrong logits at the decode key's own cnt==1 capture —
  the wrong values ARE reference's next step (one-step state skew); needs match-then-mismatch replay at chunk_size==32
  exactly; impl-independent; CPU-visible; `NO_MEMORY_PLANNER` unaffected. Core loci: `engine/jit.py` cnt==1
  double-execution, or `codegen` `to_program_cache` keying. Diagnosed + `@expectedFailure` regression
  (`TestGDNScanJitReplayRegression`); **parked** (core fix = upstream-class; policy: fork-local only).
- WY prefill perf when it works: +27-29% (28/27 vs 22/21 tok/s) — the prize behind bug-1.

## §3 Open board (in order)
1. **Artur merges PRs #25 (T4.77) + #26 (T4.78)** → bump `tinygrad-td5` + CLAUDE.md base to the new master.
2. **T4.79 state-cache standing adoption**: one Q8 validation round with `--state-cache` on the merged master
   (its E3.3 run was clean WITH cache), then add to the plist env. Optional hardening: budget STATE_CACHE_MB
   against per-device free; T4.74's c1/c2/c3 candidates now optional (branch pushed).
3. **T4.70d TP hardware bring-up — UNBLOCKED** (fault #5's class retired): r=0.70 `tp:` map round with the
  forensics ring armed; T470_TP_DESIGN.md has the allocation model.
4. **T4.73a bug-1 next levers**: scale the real-generate experiment toward serve conditions (quant weights, real
   48×128 geometry, more blocks, METAL) until it reproduces, or instrument `graph_split_rewrite`/replay on the live
   stack; the fix unlocks the WY auto-flip (+27-29% prefill) and possibly bug-2 insight.
5. **T4.66b/c MTP perf** (batch the draft chain, kill the REDO; correct but 2 vs 7 tok/s) — after 2/3.
6. Housekeeping: worktrees t473/t473fix/t476/t477/t478 + e3-* branches are evidence — prune only after merges.

## §4 Standing rules (unchanged)
Never kill -9 any NV process or TinyGPU server; stop/restart ONLY via `pooled-serve.sh` (SIGTERM); no KeepAlive
supervisors; no second DEV=NV process; evidence-before-teardown on any fault; fresh client = the validated recovery
(6-for-6 now); fork-local only (no upstream PRs/reports); agents are CPU/NULL-only, never touch :8081/tmux/launchctl,
commit locally, never push; push docs+evidence branches via the explicit HTTPS URL (`origin/*` stale by construction);
gates: `CHECK_OOB=1 DEV=CPU PYTHONPATH=. .venv/bin/python -m pytest <area> -x -q` serial + `SPEC=2 DEV=NULL test/null/`
+ mypy + ruff (no pipe-tails); workflow-file pushes need Artur.

## §5 This handoff supersedes HANDOFF_2026-08-31.md (kept as the 3.8-usability-chain-era history).
