# HANDOFF 2026-09-13 — checkpoint before the eGPU dock is disconnected (long-context kernels live, PR #45 merged)

**Entry point for the next session.** Read this, then `HANDOFF_2026-09-12.md` (§6 = the Hermes-run analysis and the speed curves, §1–§5 = the card path), then `TASKS.md`.

## 0. State at the checkpoint (09-13 ~10:40)

- **Code:** fork `master` == serving branch `integration/t6` == `f3c3b7fe8 Merge pull request #45 from arttarawork/integration/t6` (PR #45 merged 09-13, 51 checks green; the post-merge fold is done). Task worktrees for T4.106/T4.107a/b/T4.94 removed; their branches are merged. Docs branch `memory` pushed.
- **Server:** stopped deliberately for the dock disconnect (`pooled-serve.sh stop`, SIGTERM path). The login LaunchAgent will not start it while the dock is absent (start fails, not retried) — that is expected.
- **Hermes:** gateway up, idle since 09-12 12:45; with the server down and no fallback a Telegram turn fails visibly until the card is back. The live Telegram session sits at ~120k tokens (the 09-12 DeepSeek detour) — `/new` is the cheap option.
- **Host:** colima VM 8 GiB (Firecrawl fixed, T4.109), swap ~3.3 GB, memory ~93 % free.

## 1. Reconnect procedure

1. Plug the dock in; wait until TinyGPU shows the card (the `TinyGPU … server` process is always there; never kill it).
2. `SKIP_IDLE=1 ~/Documents/tinygrad-bench-data/deploy.sh` — the defaults below, READY in ~5 min (`curl localhost:8081/v1/models`). Or log in again: the LaunchAgent starts the same recipe.
3. Restart any Hermes CLI session. Telegram needs nothing.
4. Sanity: a 2.6k-token prompt should log `prefill: ~118 tok/s … gen: ~24 tok/s`; a 21.6k prompt `prefill: ~109 … snap: ~2.4s … gen: ~22`.

## 2. The standing recipe = the plist `com.artur.pooled-serve` (these are the defaults; `deploy.sh` carries the same)

`POOLED_MODEL` = Qwen3.8-27B **Q4_0** (`/Users/artur/models/qwen3.8-27b-q4/Qwen3.8-27B-Q4_0.gguf` + its `mmproj-BF16.gguf`), `POOLED_DMAP` 0-63:NV, `POOLED_CTX` **262144**, `POOLED_FLAGS` `--state-cache --mmproj …`, tree `integration/t6`.

| env | value |
|---|---|
| `BEAM_CACHE_ONLY` | `1` |
| `BEAM_CACHE_ONLY_DEVS` | `NV,METAL` |
| `DEFAULT_TEMPERATURE` | `0.6` |
| `DEFAULT_TEMPERATURE_THINK` | `1.0` |
| `GDN_NV_FUSED` | `0` |
| `GDN_NV_FUSED_DECODE` | `1` |
| `GDN_SCAN_IMPL` | `2` |
| `JIT_SHARED_ARENA` | `1` |
| `KQUANT_STAGE` | `0` |
| `KV_INT4` | `1` |
| `LOOP_NUDGES` | `0` |
| `LOOP_REPEATS` | `3` |
| `LRU` | `1` |
| `MIN_P` | `0.05` |
| `NV_CUSTOM_ATTN` | `1` |
| `NV_CUSTOM_QUANT` | `1` |
| `NV_DISPATCH_RING` | `64` |
| `POOLED_CTX` | `262144` |
| `POOLED_DMAP` | `0-63:NV` |
| `POOLED_FLAGS` | `--state-cache --mmproj /Users/artur/models/qwen3.8-27b-q4/mmproj-BF16.gguf` |
| `POOLED_MODEL` | `/Users/artur/models/qwen3.8-27b-q4/Qwen3.8-27B-Q4_0.gguf` |
| `POOLED_TMUX` | `1` |
| `POOLED_TREE` | `/Users/artur/Documents/tinygrad-t6int` |
| `STATE_CACHE_DEVICE` | `METAL` |
| `STATE_CACHE_MB` | `4096` |
| `STREAM_LOG` | `/Users/artur/.hermes/stream-viewer/pooled-stream.log` |
| `THINK_BUDGET` | `16384` |
| `VISION_DEVICE` | `METAL` |

Why each matters: `NV_CUSTOM_QUANT` (11-format dp4a gemv + WMMA gemm), `NV_CUSTOM_ATTN` (T4.107 decode split-KV + prefill tiles — the long-context speed), `GDN_NV_FUSED_DECODE` (fused GDN scan at T=1), `KV_INT4` (18 KB/token; int8 does not fit 262144), `JIT_SHARED_ARENA` (one planned arena per device: ~3 GB back, needed for the full window), `LRU=1` (the LRU allocator ON — `LRU=0` costs ~25 % of decode; the METAL snapshot leak is fixed by T4.106 instead), `STATE_CACHE_DEVICE=METAL` + `STATE_CACHE_MB=4096` (a ~196k-token boundary snapshot is ~3.6 GB), `KQUANT_STAGE=0` (no staging copies), `BEAM_CACHE_ONLY=1` (cached, verified BEAM winners only), the sampler/thinking knobs (`DEFAULT_TEMPERATURE` 0.6, `DEFAULT_TEMPERATURE_THINK` 1.0, `MIN_P`, `THINK_BUDGET` 16384, `LOOP_REPEATS` 3 / `LOOP_NUDGES` 0), `STREAM_LOG` (the live thinking viewer).

## 3. Hermes settings that matter (config.yaml; backups `.bak-20260913-1005-decisions`, `.bak-20260912-2026-q4max`)

| key | value |
|---|---|
| `model.default` | `Qwen3.8-27B` |
| `Qwen3.8-27B context_length` | `262144` |
| `compression.threshold_tokens` | `196608` |
| `compression.threshold` | `0.5` |
| `compression.context_timeout_seconds` | `3600` |
| `compression.context_total_ceiling_seconds` | `10800` |
| `auxiliary.compression.timeout` | `7200` |
| `fallback_providers` | `[]` |
| `tool_loop_guardrails.hard_stop_enabled` | `true` |
| `providers.lmstudio.request_timeout_seconds` | `43200` |
| `providers.lmstudio.stale_timeout_seconds` | `7200` |

Also: `providers.lmstudio` = the local server; every `auxiliary.*` block points at `Qwen3.8-27B` on :8081 (compaction, title generation, vision); `/reasoning medium` is the recommended per-conversation habit (`high` maps to the template's `xhigh` with a 64k budget — up to an hour per think block even at today's speeds).

## 4. Measured, current recipe (on the wire unless noted)

| context | prefill tok/s | decode tok/s |
|---|---|---|
| 2.6k | 118 | 24 |
| 21.6k | 109 | 22 |
| 61k (validation, per 32-token chunk / per step) | 507 ms per chunk ≈ 63 | 62 ms ≈ 16 |
| 196k cutoff (extrapolated) | ~40 | ~13 |

Compaction at 196608: the summariser call is 7 min typical, was 60 min worst case before the prefill kernel (now well under 30). A new user turn pays the state-cache store: ~0.1 s per 1k tokens (T4.112). Quality: 8/1152 top-1 flips from the attention kernels vs the same recipe without them; Q4_0 itself ~2.3–2.5 % vs Q8.

## 5. Open list (all recorded in TASKS.md)

- **T4.113** WMMA (tensor-core) prefill attention — noted, waiting (Artur: "Wait, but note it down").
- **T4.93b** real-prompt quality gate — needs 20–30 prompts exported by Artur (the session store is off-limits to Claude).
- **T4.94b** MTP loop rewrite — parked (measured 0.9–1.5 tok/s vs 24 plain; ceiling ~1.5–1.8× at short context).
- BEAM refresh once, overnight, after any kernel change (a few % on prefill at most).
- Chat harness: keep Telegram for mobile; the Hermes TUI over Tailscale/SSH for long coding turns; optionally DeepSeek Harness (`dsh`, assessed 09-13: a coding-agent harness with a web UI that can drive :8081 as a custom OpenAI-compatible provider and use a separate summarization model) as an experiment; or point Hermes's `auxiliary.compression` at DeepSeek V4 Flash if summaries may leave the machine.
- Parked/dropped: IQ2/Q2_K formats, T4.111 (system-prompt-boundary snapshot), T4.103a/b (superseded by T4.107b).

## 5b. DeepSeek Harness (added 19:30 after the reconnect)

Installed as the coding harness on the same server (details: `~/.claude/CLAUDE.md` § DeepSeek Harness): `scripts/dsh-web.sh` starts the web UI on 127.0.0.1:3080 (token in `~/.dsh/web.log`); settings in `~/.dsh/settings.yaml` (route `pooled`), default model + compaction (Qwen as summarizer) in `~/.dsh/cordis.patch.yml`, rules in `~/.dsh/AGENTS.md`. Not a LaunchAgent — restart by hand after a reboot, after the model server is up.

## 5c. 09-14 additions

- **T4.111 live** (integration/t6, not yet in a PR): system-prompt boundary snapshots — new sessions sharing a prefix answer in ~2 s instead of re-prefilling 8–13k tokens; validated token-identical on the card.
- **dsh `lean` preset** is the web default (`~/.dsh/.agent-presets/lean`); the headless profile ignores presets.
- The MTP bench, T4.112 and the attention kernels are on master (PR #45); T4.111 + the AGENTS.md fork block are the next PR.

## 5d. 09-15: small models on the Mac beside the 27B (T4.114/T4.115; Artur: "small, quick models we can run on the Mac simultaneously that are comparable to Haiku or Sonnet")

**Research verdict (calibration: Claude Haiku 4.5 = 73.3 SWE-bench Verified, Sonnet 5 = 72.7).** Of the four names Artur gave: *Nanbeige4.2-3B* is real (looped 3B, 63.6 SWE-bench Verified, Apache 2.0); *"Tiel Coder"* is a requant of Ornith-1.5-35B-A3B (79 claimed, 21 GB at 4-bit — a 27B alternative, not a companion); *Laguna S 2.1* is 118B/8B (too big; poolside's small one is Laguna XS 2.1, 33B/3B, ~19 GB); *Ling-3.0-tiny* is 7.9B MoE / 1.3B active, 256k, native tool calls, no published SWE-bench score (Artificial Analysis: composite index only, "very verbose"). The one that matters: **Ornith-1.0-9B** (dense, on Qwen3.5, MIT, 256k, **69.4 SWE-bench Verified**, Terminal-Bench 2.1 43.1). Nemotron (asked 09-15): the only ones that fit 18 GB (Nano-4B, Nano-9B-v2) publish no SWE-bench number; 3.5-Lightning-30B-A3B scores 51.6 but is ≥19 GB — none belongs in the lineup.

**Measured on this Mac (llama.cpp Metal, git HEAD 4c9233c — the Homebrew 10250 build lacks `bailingmoe3` = Ling; built in `~/src/llama.cpp/build`):**

| model (file in `~/models/small/`) | 2k prompt prefill / decode | 20k prompt | RSS (standing ctx) |
|---|---|---|---|
| Ornith-1.0-9B Q4_K_M (5.5 GiB) | 312 / 21.2 tok/s | 307 / 18.1 | 6.7 GB at 32k |
| Ling-3.0-tiny Q8_0 (7.8 GiB) | 1364 / 56.5 | 929 / 54.2 | 8.7 GB at 3 slots × 32k; 3 concurrent requests: 34–37 tok/s each, 107 aggregate |
| Nanbeige4.2-3B Q8_0 (4.1 GiB) | 367 / 16.6 | 207 / 12.2 | 15.6 GB at 64k (the loop multiplies the compute buffers; slower than the 9B — not kept running) |

Both small servers up together next to the 27B server (4.9 GB) and colima (6.5 GB): PhysMem 33 GB used, 18 GB wired, compressor 6.7 GB, swap 2.9 GB used — **at the edge**; the default is Ornith standing, Ling started when a fan-out is wanted (`small-serve.sh start ling`). Tool calling verified on both (`/v1/chat/completions` with `tools` → `tool_calls`).

**Wiring.** Launcher `~/Documents/tinygrad-bench-data/small-serve.sh start|stop|status [ornith|ling|nanbeige]` (tmux `small-<m>`, logs `~/.hermes/logs/small-<m>.log`, no LaunchAgent, not a `DEV=NV` process). dsh: providers `ornith` (:8082) and `ling` (:8083) in `~/.dsh/settings.yaml` + `subagent-model-selection` policy (ornith, ling, pooled); the lean preset gained `tool-subagent-control`, `.../list-agents` and `tool-subagent` (spawn, continuable, default route ornith/Ornith-1.0-9B, effort off, 8192 max tokens) — the 27B designs, the 9B executes; the child joins the parent's preset (same tools). Hermes: the eight cheap `auxiliary.*` blocks (skills_hub, approval, mcp, title_generation, triage_specifier, kanban_decomposer, profile_describer, curator) → Ornith on :8082 so they no longer queue behind the main turn on the single-flight :8081; compression, vision, web_extract stay on Qwen (backup `config.yaml.bak-20260915-small-aux`). Bazzite kit rebuilt (providers, preset rows, LocalForward 8082/8083). **02:20–02:50 lessons:** (1) both small servers up next to the emulated SWE-bench container pushed swap to 4.1 GB and macOS killed background jobs — **one small server at a time while a benchmark runs** (the queue stops/starts them; Ornith is restored at the end). (2) A killed client does not stop a non-streaming llama-server request: the first (thinking-on) Ling run left an orphaned generation that ran past 10,000 tokens at 54 tok/s and blocked the restarted run behind it — `small-serve.sh` now passes `-n 4096` (env `SMALL_NPRED`) and the benchmark overlay sets `max_tokens: 4096`; restart the server (`small-serve.sh stop/start ling`) to kill an orphan. (3) Ling in thinking mode wrote 6k-token think blocks per step; the benchmark runs it in instant mode (`ling-instant.yaml` → `extra_body.chat_template_kwargs.enable_thinking: false`, verified through litellm: 156 → 41 completion tokens, 0 reasoning). (4) SWE-bench images are x86_64 only for most instances (arm64 exists for some): `--platform linux/amd64` under colima's emulation runs Python ~3× slower than native; images are 4–11 GB each, so `janitor.sh` deletes each one after its instance. Ling steps at ~9 s (≈310 output tokens); expected ≈8 min per instance at the 50-step cap.
**09-15 08:20–12:20 results so far (patches submitted / 20, before the official evaluation):** Ornith-1.0-9B **12** (1 step-cap, 6 context overflows at the 32k benchmark window, 1 format loop; 13–40 steps each), Ling-3.0-tiny instant **1** (14 step-caps, 3 format loops, 2 overflows — it explores well, twice applied the right edit, then never submits or repeats one diagnostic 29×), Nemotron-3-Nano-4B **4** (11 format loops, 33 tok/s), Nemotron-Nano-9B-v2 running without thinking (`nemotron-nothink.yaml`: `/no_think` system prompt; with thinking it wrote 1.3k reasoning tokens per step at 16 tok/s). Side-by-side action lists: `/tmp/claude-501/compare.txt` (session scratchpad) and the trajectories under `runs/<model>/<instance>/`. **Ornith-1.5-9B** (Artur 12:00: "are we considering Ornith 1.5 9B?"): official GGUF, MIT, thinking model, published SWE-bench Verified 70.6 / Terminal-Bench 2.1 46.2 / SWE-bench Pro 47.5 (1.0: 69.4 / 43.1 / 42.9); measured here 324 / 22.6 tok/s at 2k, 308 / 20.8 at 20k, 6.8 GB — identical to 1.0 → **standing worker on :8082 since 12:20** (launcher `ornith` = the 1.5 file, `ornith10` = the old one on :8088; dsh route id and Hermes aux blocks renamed `Ornith-1.5-9B`). Queue part 2 (`queue2.sh`) finishes the 9B leg, evaluates all four, restarts Ornith; `queue3.sh` then runs `kl_test.sh` (KL divergence of Q6_K/Q5_K_M/Q4_K_M vs Q8_0 on tinygrad source) for the quant choice — published data (AtomicChat KLD table, a 60-task coding test) points at **Q5_K_M** (KLD 0.030 / 92.8 % top-1 vs Q4_K_M ~0.05 / ~90 %; Q4_K_M scored lowest functionally). **Evaluation path validated 12:24** (Ornith's django-15572 patch → resolved: True) after three fixes in `evaluate.sh`: swebench 5.0.2 needs the `SWE-bench/SWE-bench_Verified` dataset (rows carry `image`), one instance per harness call with `docker rmi` + `colima ssh -- sudo fstrim -a` between (the harness keeps every 4–11 GB image; the sparse colima disk had reached 73 GB for 36 GB of data — one `fstrim -av` returned 54 GB), and a CLI `docker pull --platform linux/amd64` before each call (the harness's SDK pull has no platform and 404s on the arm64 daemon; it reuses a local image).
**15:10 evaluated:** Ornith-1.0-9B **12 / 20 resolved** (every submitted patch passed the official harness; the 8 misses were 6 context overflows at 32k, 1 step cap, 1 format loop); Ling-3.0-tiny instant/greedy **1 / 20** (its one patch passed); Nemotron legs cut (4B: 4 patches; 9B v2: 3/20 run, 1 patch) — Nemotron is out. **Quant KL test on this Mac** (`kl_test.sh`, 102 KB of tinygrad source, 24 × 2048-token chunks, each quant vs Q8_0): same-top-token Q6_K 97.2 %, Q5_K_M 96.1 %, Q4_K_M 93.4 %; 99th-pct KLD 0.065 / 0.134 / 0.409; mean Δp −0.009 / −0.007 / −0.124 % — agrees with the published AtomicChat table (Q4_K_M is the big step) → **Ornith-1.5-9B Q5_K_M is the standing worker** (launcher `ornith` → the Q5_K_M file; +0.9 GB). Ling rerun with the card's sampling (temp 1.0 / top_p 0.95 / top_k 20, instant, 1 × 64k) running since 14:06 (`runs/ling-recommended/`): 5/20 so far, 1 patch — same shape as the greedy run.
**16:10 memory (Artur: option 2):** the 36 GB split at 15:00 = colima VM 9.0 GB (cap 8 GiB; Firecrawl 3.6 GB inside, api 2.8 GB idle; guest page cache) + small server 7.0 GB + :8081 server 5.0 GB + compressor 3.9 GB, 0.3 GB unused, swap 4.3 GB. Apple's VZ has no balloon: dropping 3.5 GB of guest cache left the VM's host RSS at 9 GB, so only the cap moves it. `mem_window.sh` (tmux `mem-window`, log `mem_window.log`) waits for queue4 + 10 min of :8081 idleness, then `pooled-serve.sh stop` → `colima stop` → `colima start --memory 6 --cpu 8` → Firecrawl `compose up -d` with `mem_limit: 3500m` on the api (override backup `.bak-20260915`) → `deploy_start.sh`. **`deploy_start.sh` was STALE** (ctx 131072, LRU=0, cache 2048, no shared arena / attention kernels — it had not followed deploy.sh since 09-12): regenerated from deploy.sh 16:10 (`.bak-20260915-stale`); never copy the two by hand again — `sed restart→start` from deploy.sh.
Open: T4.115 = Ling on a 20-instance SWE-bench Verified subset vs Ornith (harness in `~/Documents/tinygrad-bench-data/swebench-small/`), which decides whether Ling's 3 slots replace the 9B as the subagent model.

## 6. Rules that changed this round (already in the repo CLAUDE.md / memory)

- Never restart the server while Hermes is mid-turn except an emergency (a restart used to fall over to DeepSeek; the fallback is gone now, so it fails visibly instead).
- Card windows: every card measurement stops the server first (`pooled-serve.sh stop`), runs from a worktree with the recipe env, restarts via `deploy_start.sh`; the scripts in `~/Documents/tinygrad-bench-data/scripts/` are the templates.
- A colima restart kills the server's NVRTC compile container: only in a server-down window.
- `ls ~/Desktop` from this terminal blocks on a macOS folder-access dialog; copy into `~/Documents` instead.
