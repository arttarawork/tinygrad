# HANDOFF_2026-08-31.md — THE entry point (supersedes HANDOFF_2026-08-27.md)

## §0 State in one paragraph
The pooled tinygrad server is THE local model (Hermes "Pooled (localhost:8081)"): **Qwen3.6-35B-A3B Q8_0 @192k**, serving tree
`tinygrad-td5` on fork master **`7ceb05951`** (= the PR #21 merge). Between 08-27 and 08-31 the fork gained, via PRs #9–#21 (all merged):
chunked hybrid prefill (T4.55-58, 35B prefill 46→98 tok/s flat), serve hardening + splice, UD-quant decoders (T4.60), the Qwen3.8-27B
usability chain — scan parity harness (T4.61), head-group scan split (T4.62), MTP nextn loading (T4.63), greedy+sampled speculative
decoding + `--mtp` (T4.64/65), chunk-64 coverage (T4.68), cross-session prefill state cache `--state-cache` (T4.67), and the chunkwise
WY-form scan behind `GDN_SCAN_IMPL` (T4.69a) — plus CI test-cost fixes (T4.71). A full GPU battery (08-31) measured everything; see §2.

## §1 Read order
1. This file → `TASKS.md` (task list + Status log — the per-round truth). 2. `Q38_PERF_PLAN.md` (P1–P5 levers; §status below supersedes its
sequencing). 3. `MTP_SCAN_PLAN.md` (wave-1 specs, now historical). 4. `NV_LLM_DESIGN.md` §1.5 + `memory.md` for deep background.

## §2 Measured truth (pooled METAL M3 Pro 36GB + RTX 3090 @ AOOSTAR AG02)
| | Qwen3.6-35B Q8 @192k (standing) | Qwen3.8-27B Q8 @65k (TD.6 map) |
|---|---|---|
| prefill 5.9k/16k | 98 / 94 tok/s (flat) | 22 / 21 loop · **28 / 27 WY (+27-29%)** |
| decode short/16k | 28 / ~22 (192k map; 33 was the 128k-map datum) | 7-8 / 6 |
| cold 19k Hermes turn | ~3.4 min (follow-ups ~2 s, splice) | ~15 min → seconds-class WITH the state cache (measured hit: `in: 5872 +26`) |
Battery verdicts: state cache WORKS; chunk-64 CLOSED (identical perf, token-identical output — stay 32); WY prefill is the real 3.8 lever
but WY decode at T=1 is pathologically slow → T4.69b gate; **MTP is GPU-BLOCKED** (~1 tok/s: python-int draft start_pos ⇒ per-position
trace/schedule; ALSO the map must budget the ~0.5 GB nextn head on NV — first attempt OOM'd, fixed by moving 4 tail DN blocks to METAL).
**Fault pattern: the 3rd fault LANDED (2026-08-31 evening, during the first-ever GPU speculative-decode requests — recoverable class, evidence `t460/fault3_evidence.txt`) ⇒ T4.72 investigation task is OPEN (see TASKS.md).** MTP GPU validation is paused behind it.

## §3 Open board (in order)
- **T4.69b** (agent in flight): gate `GDN_SCAN_IMPL=2` to chunks (`T_pad>1`), loop for decode → then Fable re-measures on hardware and
  decides the auto-flip. Ships +27-29% 3.8 prefill.
- **T4.66a** (agent in flight): bind `MTPHead.draft`'s start_pos to a Variable (one capture, replayed per position) → then Fable re-runs the
  MTP battery phase (projected 1.5-2× 3.8 decode). Later: T4.66b kill the partial-accept REDO (per-position hiddens), T4.66c per-step draft
  hidden (acceptance quality).
- **T4.70** TP design (Fable/Fable-fork — NOT Sonnet): intra-layer tensor parallelism, decode ceiling ~15-20 tok/s; Q38_PERF_PLAN §T4.70.
- State-cache standing integration: add `--state-cache` (+ cap) to the pooled ritual after a 35B-scale measurement; snapshot budget matters
  at 192k (KV lives mostly on NV).
- Worktree prune list: t455/458/461/463/464/467/468/469/471 (branches merged); keep td5 (serving), and wave-3's t469b/t466 while open.

## §4 Standing rules (unchanged unless noted)
Never kill an NV process or TinyGPU server (SIGTERM via `pooled-serve.sh` is the one validated exception; stray-pid catch is built in now).
Serving ritual + all knobs: `~/CLAUDE.md` "Pooled model" (new: `POOLED_BEAM_TIMEOUT`, `POOLED_MTP`, `POOLED_ENV`, `POOLED_FLAGS`).
Fork-local only; no upstream PRs/reports. `origin/*` refs stale by construction — verify via `gh api`; push via the explicit HTTPS URL;
**workflow-file pushes need Artur** (no `workflow` scope on the token). Gates: `CHECK_OOB=1 DEV=CPU PYTHONPATH=. .venv/bin/python -m pytest
<area> -x -q` serial + `SPEC=2 DEV=NULL test/null/` + `test/unit/test_llm_{device_map,server,sampling}.py` (jit-key literals!) + mypy + ruff.
CI constraints learned the hard way: fork runners ≈3× the M3 (keep tests ≲60 s local); deep unrolled-scan graphs (grouped c32/c64) crash
xdist workers natively — never add them to CI; tinygrad core stays numpy-free at import; never `mypy | tail` (pipe eats the RC); in chained
shell commands `cd` back before repo-relative heredocs.

## §5 This handoff supersedes HANDOFF_2026-08-27.md (kept as the TD.5-era history).
