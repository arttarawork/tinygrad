# HANDOFF — 2026-08-26 ~13:00, revised ~15:00, consolidated ~17:45 (written after the 2nd kernel panic; read this FIRST)

## 0. Situation in three sentences
The eGPU dock project executed Artur's full 8+6-task queue (+6 emergent tasks — 20 rows in §1) over 2026-08-25/26 and every task is done,
documented, and pushed — but the host **kernel-panicked twice** (01:15 and 12:06, identical
`dart-apciec2 AppleT8110DART.cpp:2183` signature) during dock work. The first panic was diagnosed and a
fix (T4.37) shipped and hardware-verified — the second panic proved that fix **necessary but not
sufficient** (see §2). **ALL DOCK WORK IS HARD-STOPPED; Artur must choose among the 3 options in §5
before anyone touches `DEV=NV*` again.** *Evening status: RCA done (`T4.40_RCA.md`), client-side remediation
40-1/40-2/40-4 verified on pushed branches, PR #3 and PR #4 both merged (master `a770d485a` = Phase 1 + T4.37 + T4.31 + upstream sync #4), PR #5
(T4.40a+b) in assembly — the §5 decisions remain Artur's.*

## 1. What is DONE (all committed; branch map in §3; details in TASKS.md status rows)
| Task | One-line result |
|---|---|
| T4.24 | 56.58 tok/s headline VALIDATED for coherence (150/150 vs llama.cpp on one prompt; BEAM not bit-deterministic across shapes — benign, documented) |
| T4.19 | experts-split divergence = benign FP drift (sel matches exactly; logit gap 0.011-0.022); 3 hops/layer confirmed; range split byte-identical |
| T4.23 | VRAM OOM never faults the GPU (proven 3 ways); fault messages now name recovery |
| T4.21 | Big-quant pooling unlocked: quantized-blob per-device placement; NV residency 6.92→2.12 GB; qwen3.6 splits load zero-swap; 16k ctx fits |
| T4.20 | kernargs slab (re-scoped to robustness — range splits need 4 islands, not ~85); experts-split crash fixed, verified live |
| T4.22 | IQ dequant fixed (bytes 63.5x→1.4-1.8x, bit-exact) — but see T4.29/T4.26 |
| T4.15 | BEAM multiplier decay is structural; found T4.27 |
| T4.16 | MATVEC heuristic fires on NV and helps MORE than METAL (nvcc Q4_0 5.25x); premise refuted |
| T4.25 | Server spawn flock (2 of 3 diagnosed defects refuted live; residual server-side race documented) |
| T4.27 | BEAM silent-fallback fixed (ungated WARNING + hand_coded fallback); generic upstream bug |
| T4.28 | PR #2 merged by Artur → fork master `cb05a6c64`, CI 51/51, 20 NV tests relocated into CI |
| T4.30 | BEAM'd truth table re-validated: only the pathological split cell was degraded (7.58→32.58); qwen3:8b 47.9 / gpt-oss 62.8 stand |
| T4.31 | Per-candidate docker container leak fixed (Renderer.__reduce__ unpickle spawn); BEAM warm 2.7x faster; **on master since PR #3** |
| T4.37 | Bus-master cleared on fault (3 sites + try/finally); hardware-verified; **on master since PR #3**; covers A1 — A2 needed T4.40(b) (§2 RCA) |
| T4.32 | Merged tree validated on hardware (byte-exact NV+METAL, pooling 32/32 + 129/129) |
| T4.34 | Fault storm diagnosed structurally: search-time BEAM-candidate OOB (DeltaNet scan kernels prime suspect); 0 live repros in 3 tries; correlates with memory-starved searches |
| T4.29 | IQ real model: bytes 26x down but **2.2-2.6x SLOWER even BEAM'd** — T4.22's synthetic verdict refuted; coherence bit-exact |
| T4.35 | 56.58 headline **NOT reproducible cold** (2/2 independent failures at the VRAM wall); publish only with the warm-cache precondition; runs 2-3 (768→4096 recipe) never attempted |
| T4.26 | Decision: **ship IQ4_XS alone upstream; IQ3_XXS stays fork-local**; packed-word tightening shipped (compile 3.1x faster at CI width); no fusable-gather primitive exists; upstream rangeify unchanged |
| T4.33 | Upstream sync #4 (`27f56467f` = phase1b + upstream `4bdc86513`, 77 commits) — **re-validated from scratch (zero regressions/slips, §4.1) and MERGED (PR #4 → master `a770d485a`)** |

## 2. The panics (full detail: `T4.36_DART_PANIC.md`, TASKS rows T4.36/T4.37/T4.40; logs committed as `kernelPanic.txt`, `kernelPanic2.txt`)
> **RCA VERDICT (2026-08-26 PM, `T4.40_RCA.md`, commit `161014c97` on `memory`) — supersedes the mechanism-#2
> hypothesis below.** Root cause = **M-A "orphaned-session teardown"**: a process that exits without a
> *successful* GSP unload while PCI bus-master is set gets its whole session's host IOVAs unmapped by the
> server (`server.c cleanup()`→`IOServiceClose`→`CompleteDMA`, no quiescing), and the still-running GSP-RM
> then DMAs into them → DART REQUIRE. Both panics land ~10 s after session start = the driver's universal
> 10 s RPC timeout → raise → exit → unmap. **Panic 1 = A1** (fault → unload RPC times out at exit → pre-T4.37
> tree exits with MASTER on); **T4.37 already covers A1**. **Panic 2 = A2, newly identified and UNCOVERED**: a
> failed *device init* (a 10 s boot-RPC timeout) raises out of `NVDevice.__init__` → no unload, no T4.37 site
> runs, `get_available_devices` swallows it → the session stays armed until the pytest process exits ~12:06:3x.
> The opener was the **unvalidated T4.33 merge tree**, whose first-ever NV open executed upstream's new
> init-time RPC `NV2080_CTRL_CMD_FIFO_GET_DEVICE_INFO_TABLE` (a real new call site added by `4fd4eafb2 nv:
> hevc` — verified against the diff; the constant pre-existed, the *call* is new) = the plausible A2 trigger
> (H7, unverified). **No `pkill` was involved in either panic** (transcripts: the only pkill of the day was
> 08:18, T4.37 step B, which survived) — T4.36's pkill-hazard story is a real latent A3 case that never fired.
> H1 ("two servers + re-enabled mastering") demoted to enabler. **Fix set: 40-1 clear MASTER on any exception
> in `NVDevice.__init__` after the MASTER set (~4 lines, covers A2); 40-2 clear MASTER when `fini()` *raises*,
> not only on `is_err_state` (~3 lines, A1-without-events); 40-3 halt the old GSP falcon + verify
> `RISCV_CPUCTL.active_stat==0` before re-enabling MASTER in `_early_ip_init` (needs the §7 hardware pass);
> 40-4 = T4.40(a); 40-5 = the DEXT-side quiesce-before-unmap ask (the only cover for a hard
> kill / crash — draft report in RCA §8, Artur's call).** **STATUS 2026-08-26 evening: 40-1 ✅ + 40-2 ✅
> (T4.40b `57a02a06a`, verified) + 40-4 ✅ (T4.40a `940f65d79`, verified) — the entire GPU-free client-side
> remediation is DONE (A1 covered by T4.37+40-2, A2 by 40-1, the multi-server enabler by 40-4); only 40-3
> (hardware) and the 40-5 upstream report remain.** The single missing discriminator for
> A2: whether the 12:06 probe's init actually *failed* (log tail lost). **T4.33 validation (done) confirmed the
> A2 *precondition*** — a `get_available_devices()` NV construction during pytest collection, on BOTH trees —
> but the baseline tree probed at 11:57:31 WITHOUT panicking, so a plain probe isn't sufficient: what
> distinguishes the 12:06 open (upstream's new hevc RPC timing out, vs. accumulated armed-session state) stays
> lost to the log tail. The A2 mechanism and fix 40-1 hold regardless; H7 is neither confirmed nor refuted.

- Mechanism #1 (panic 1, 01:15): GSP-RM's message queues live in HOST sysmem (`nv/ip.py:367`). A GPU fault
  storm = GSP DMAing fault events into them at high rate. Killing the TinyGPU server (our old recovery)
  tears down those DMA mappings while the GPU still has PCI bus-master → one GSP DMA into an unmapped
  IOVA → `AppleT8110DART REQUIRE` → host panic. T4.37 fixed THIS path: clear MASTER on fault
  (`PCIIface.sleep`/`on_device_hang`/`device_fini`-finally), recovery hint = fresh client, never pkill under fault.
  *Log evidence (15:00 revision): the panic came 11 s after the fresh client's attach (01:15:30.9 `bar mapping 0/1/3`),
  i.e. during its clear→FLR→boot, not in the pkill window itself — see #2.*
- Mechanism #2 (panic 2, 12:06:34) — **as first written at 13:00: a hypothesis, not an RCA** — `_early_ip_init`
  (`nvdev.py:127`) unconditionally re-enables bus-master on every fresh client open; with ≥2 servers alive (T4.38
  leak) one teardown unmaps GSP host queues while another freshly-inited client has mastering on.
  **REVISED ~15:00 FROM EVIDENCE.** The unified log survives reboots and the DEXT logs every client open, cfg
  write, reset and DMA map (never disconnects/unmaps) — extract committed as `ulog_tinygpu_2026-08-26.txt`; the
  session transcripts (`~/.claude/projects/-Users-artur-Documents-tinygrad/d9cf3c0c-*/`, incl. `subagents/`)
  timestamp every command. Timeline: **11:47:54** T4.35 run-1 client opens (clear→reset→MASTER=7→GSP boot) ·
  **11:50:00 fault → T4.37 clears MASTER (cfg 7→3)** (the run "faulted ~11:57" only in that the process exited
  then) · **11:57:31** T4.33's *baseline* gate run probes NV (`test_llm_device_map.py:194` at collection) against
  the wedged-session server → spawns a new server (T4.38) · **11:57:42** the faulted run exits (its unload RPC had
  timed out ⇒ that GSP-RM was never unloaded; `device_fini` re-clears MASTER) · **11:57:43** the probe client maps
  BARs but never resets (failed/blocked before `_early_ip_init`) + a second racing server spawns · **12:00:30** the
  T4.35 agent's post-fault "fresh client health check" does clear→**reset (FLR)**→MASTER=7 (12:00:31.089)→full
  GSP boot→exits healthy (MASTER stays 7 by design) · **no DEXT event for 6 min** · **~12:06:20** T4.33's
  *post-merge* gate run (PID 17500, on the UNVALIDATED merge tree `27f56467f` — the first time that tree ever
  opened NV) starts and reaches the same NV probe within seconds · **panic 12:06:34**. **No `pkill` of any server
  was issued between 08:18 and the panic** (all transcripts searched). Panic 1 has the same shape: fresh-client
  attach 01:15:30.9 → panic 01:15:41 (its clear/reset lines are missing = log loss in the final seconds).
  ⇒ **Both panics hit ~10 s after a fresh client began `_early_ip_init`'s clear→FLR→boot on a GPU whose last
  faulted GSP-RM session was never unloaded** — not at a random teardown moment. The "stale session with mastering
  re-enabled, torn down by another server" story is NOT what the log shows: the 12:00:30 reset was real and the
  session alive at 12:06 was fresh and healthy. **Open, for T4.40 step 0:** (i) does `RemoteCmd.RESET` (an FLR,
  `TinyGPUDriver.cpp:191`, HotReset fallback) plus `ip.py:196 reset(falcon, riscv=True)` actually halt a *wedged*
  GSP-RM — `ip.py:194` *requires* WPR2 to survive the FLR and `_early_ip_init` never re-checks anything
  (`nvdev.py:121-127`, fixed 0.1 s wait); (ii) which host IOVAs the boot hands the GSP, and whether a not-halted
  old GSP-RM can still DMA to its previous session's (now unmapped) queues during the new boot; (iii) the T4.33
  merge DID alter the NV path — `nv/ip.py` +17/−6, `ops_nv.py` ±25, `support/hcq.py`, `memory.py`, `system.py`,
  `graph/hcq.py` (upstream `4fd4eafb2 nv: hevc`, `e8a8d99b9` profiler keys; T4.37's five clear sites survive) —
  whether that boot-path change is implicated in panic 2; (iv) what the DEXT tears down on client disconnect vs.
  server exit.
- **T4.40 (open, CRITICAL)** = the real fix set — **step 0 (dispatched 2026-08-26 ~15:00, GPU-free): RCA from the
  DEXT/driver source + the evidence above → `T4.40_RCA.md`; (a)-(c) are the pre-RCA design and may change** — (a) single-instance server discipline enforced at open
  (no stale servers, ever — supersedes T4.38); (b) don't re-enable mastering while a stale GSP session
  exists / controlled reset must tear down the old session first; (c) never open NV from a second process
  while a measurement or live fault exists. Upstream prior art & constraints in `T4.36_DART_PANIC.md`
  §Upstream (PR #16007 merged precedent; #16536 teardown-clear broke CI; geohot closes dock-panic issues
  unless self-contained; nimlgen wants faulting addresses/RCA, not workarounds).

## 3. Branch / worktree map (all pushed to arttarawork/tinygrad unless noted)
| Ref | Contents | State |
|---|---|---|
| `master` = `3220d6e3d` | PR #5 (= sync #4 + the full client-side panic remediation T4.37/40-1/40-2/40-4) | merged by Artur; **the task-branch base** |
| `integration/phase1b` = `b37c792c6` | (was: master + T4.37 + T4.31) | **RETIRED — content on master via PR #3** |
| `task/T4.33-upstream-sync` = `f62109c76` | merge `27f56467f` + validation notes `cada75407` + fork line-cap 27000 | pushed; **PR #4 MERGED → master** |
| `task/T4.37-quiesce-on-fault` @ `73561c88a` | the T4.37 fix + fini-fix + verification harness `extra/t437/` + step A/B logs (= `0b618da9a` + `.pyc` untrack) | on master via PR #3; branch kept for the upstream-PR rebase |
| `task/T4.31-compile-transport` @ `05d6347b9` | container-leak fix | on master via PR #3; branch kept for the upstream-PR rebase |
| `task/T4.39-beam-inf-cache` @ `3379a0831` | don't persist inf/fallback BEAM results to the disk cache | verified; PR-train candidate |
| `task/T4.40a-server-discipline` @ `940f65d79` | fix 40-4: never spawn beside a live server; probes/xdist never spawn | verified; PR #5 material |
| `task/T4.40b-quiesce-init-fini` @ `57a02a06a` | fixes 40-1+40-2: clear MASTER on failed init / on `fini()` raise | verified; PR #5 material |
| `task/T4.41-pr3-integration` @ `3a9fa2284` | the PR #3 branch | merged; retire |
| `task/T4.44-pr5-integration` | T4.40a + T4.40b | **PR #5 MERGED → master `3220d6e3d`** |
| `task/T4.40c-halt-verify` @ `5d3d8fa70` | fix 40-3: halt-verify before re-enabling bus-master | **HARDWARE-GATED — DO NOT MERGE** until the supervised session |
| `task/T4.42-compile-cache-flake` @ `0c06d43c3` | compile-cache flake EXONERATED (6 pinning tests, no source change) | verified |
| `task/T4.43-qcom-server-cache` @ `53107aa64` | qcom per-unpickle server spawn fixed via shared `compiler_server_cache.py` | verified |
| `task/T4.26-iq-redesign` @ `de8855d26` | packed-word select tightening + `T4.26_NOTES.md` | fork-local + memo |
| `task/TD.3-pooling` | T4.14→T4.25 originals + `TD3_POOLING_NOTES.md` §0-§14 + `BENCH_NOTES.md` through T4.24 | evidence + PR sources |
| `task/T4.27-beam-silent-fallback`, `task/T4.30-beam-revalidation`, `task/T4.32-smoke`, `task/T4.34-fault-storm`, `task/T4.35-cold-cache`, `task/T4.29-iq-realmodel`, `task/T4.28-integration` | per-task evidence/notes (each has its NOTES/BENCH section) | pushed |
| `memory` | TASKS.md, this file, `T4.36_DART_PANIC.md`, memory.md, panic logs + `ulog_tinygpu_2026-08-26.txt` (`*.txt` is gitignored → `git add -f`; panic logs complete at 363 / 353 lines) | pushed |
Worktrees `/Users/artur/Documents/tinygrad-t4xx` map 1:1 to the branches; also `tinygrad-p1b` (phase1b), `tinygrad-integ` (T4.28), `tinygrad-t433-baseline` (detached at `b37c792c6` — was §4.1's gate-delta baseline; obsolete once PR #4 merges), `tinygrad-t439`/`t440a`/`t440b` (the three fix branches); `tinygrad-dock` holds `task/TD.3-pooling`; the venv lives ONLY at `/Users/artur/Documents/tinygrad/.venv`.
**Verifying "pushed": `origin/*` tracking refs are stale by construction** (SSH `origin` is interactive-only; agents push/fetch via the explicit HTTPS URL, which never updates `origin/*` — so `git status` shows `memory [ahead N]` and no `origin/task/*`; that is NOT a backup gap). Ground truth: `gh api repos/arttarawork/tinygrad/branches --paginate --jq '.[] | "\\(.name) \\(.commit.sha[0:9])"'`. **Verified 2026-08-26 14:30, re-verified ~17:45 after the PR #3/#4 updates: every row above matches GitHub.**

## 4. Loose ends (ordered by importance)
1. **T4.33 validation — DONE 2026-08-26 PM (`T4.33_NOTES.md` @ `cada75407`): VERDICT trustworthy to push**
   (Artur — the branch carries the workflow-file merge, so the push needs his token scope). From-scratch
   `DEV=CPU` re-run vs baseline `b37c792c6`: zero regressions (all gate deltas = upstream's additive tests),
   zero merge slips, rangeify canaries (T4.13/T4.22/T1.4/T4.9) byte-identical, fp16-KV kept over geohot's
   `477b57380`. It also **confirmed the RCA A2 precondition** — the original gate script lacked `DEV=CPU`, so
   `test_llm_device_map.py:194/382`'s `get_available_devices()` built real NV at collection on BOTH trees
   (baseline probed at 11:57:31 too, without panicking). *Original note, for the record:* the agent died with
   the process before reporting. **Its transcript survives** (not the scratchpad `.output` the 13:00 draft cited — that is gone):
   `~/.claude/projects/-Users-artur-Documents-tinygrad/d9cf3c0c-e9ed-4794-ba21-e3ea71b3dc79/subagents/agent-a2a7e6761f7d417e7.jsonl`
   (1 MB, 11:40-12:06). Recovered from it: **5 conflicts** (`test/unit/test_attention.py`, `engine/jit.py`,
   `llm/cli.py`, `llm/model.py`, `uop/ops.py`) resolved 11:47-11:55; a first gate attempt at 11:56 hit
   **collection errors (2 + 10) and mypy exit=2** before the merge was committed; the post-merge gate run was in
   flight at the panic. Still missing: the 13-file semantic review, gate deltas, rangeify re-validation
   (T4.13/T4.22/T1.4/T4.9). Re-run from scratch on `tinygrad-t433` against `tinygrad-t433-baseline` (detached at
   `b37c792c6`): **`DEV=CPU`, serial** — the only unit-test NV probes (`test_llm_device_map.py:194,382`) are
   `Device.DEFAULT == "METAL"`-gated, so `DEV=CPU` gates never open NV; `-n` spawns servers; exclude
   `test/device/test_hcq.py` (opens `Device["NV"]` unconditionally). Checked 14:30: no conflict markers; 137 files
   / +3143/−2526; the 13 both-sides files match the T4.33 row. **Do not trust or push the merge before this — and
   see §2: this tree's first-ever NV open is 10 s before panic 2.**
2. **T4.35 runs 2-3 never attempted**: the cold-768→warm-4096 recipe and the smaller-footprint search are
   untested (blocked by the multi-server STOP, then the panic). The verdict "(c)-leaning" could still
   upgrade to "(b) reproducible-with-precondition" — but it is DOCK WORK, gated on §5.
3. **T4.34 live capture** still missing (`DEBUG=2 BEAM_DEBUG=2 BEAM_LOG_SURPASS_MAX=1` recipe ready);
   memory-wall correlation noted (all-NV faults happened at ≤1 GB headroom; T4.29's nvcc fault had ~7 GB
   — so not purely memory). DOCK WORK.
4. **T4.39 — DONE (evening): `3379a0831`, verified** — inf/fallback results are no longer persisted; a later
   run re-searches and re-warns. PR-train candidate.
5. **T4.29's nvcc-lane row** never measured (its one nvcc attempt faulted, which was itself the wanted datum).
6. Stale-server sweeps: after ANY pytest that opens NV, check `pgrep -fl "TinyGPU.*server"` — and verify the
   matched PIDs against `/Applications/TinyGPU.app` (§7 lesson 7). T4.40(a) now prevents client-side spawn
   pileups at the source; the sweep remains as a tripwire. As of ~17:45: 0 servers, machine clean.

## 5. ARTUR'S PENDING DECISIONS (nothing below proceeds without him)
1. **Dock — DECIDED (Artur, 2026-08-26 evening): no report, resume via the hardware session.** (a) is done
   (40-1/40-2/40-4 on PR #5); **(b) is OFF** — tinygpu_releases follows up only on their own dock (#16086;
   ours is an AG02) and AI-derived PRs are not accepted upstream (#14615) — `TINYGPU_REPORT_DRAFT.md` is
   retained as the internal RCA summary, NOT filed. Consequence: 40-5/the DEXT fix will never land ⇒ **A3
   (hard kill/crash with a live session) is uncovered permanently — 'never kill an NV process or TinyGPU
   server' is standing law, not a temporary rule.** Next: **the supervised hardware session** — 40-3
   is CODE-COMPLETE on `task/T4.40c-halt-verify` (`5d3d8fa70`, DO NOT MERGE until verified; 5-point checklist
   in `T4.40c_NOTES.md` §6) + the RCA §7 protocol → dock measurement resumes.
2. **PR train — DECIDED (Artur, 2026-08-26 evening): OFF.** AI-derived PRs are not accepted upstream
   (#14615 confirmed by policy), so all 14 fixes stay **fork-local**; the accepted cost is conflict/rebase
   surface at each weekly upstream sync (the sync #4 playbook — `DEV=CPU` gates vs baseline, semantic review
   of both-sides files, re-derive the line cap — is the standing mitigation). The per-fix branches remain
   pushed as evidence/cherry-pick sources.
3. **T4.33 — DONE: PR #4 merged** (master `a770d485a`).
4. **TD.4 publishing numbers** (post-T4.30/T4.35): qwen3:8b 46.9-47.9 · gpt-oss 60.9-62.8 · qwen3.6 all-NV
   56.58 **only with the warm-cache precondition** (2/2 cold failures) · qwen3.6 split ~31-32.6 ·
   pooling hops 66-90 µs · olmoe range-split BEAM'd 69.8 beats all-METAL.
   *(Still open — the exo #1904 / Discord demo is a separate call from the PR/report decisions above;
   finalize after T4.35 runs 2-3 settle the 56.58 caveat.)*

## 6. Machine state at handoff + standing ops rules
llama-server UP (Hermes serving; window policy: may stop 22:00-19:00, check idle, restore when done) ·
colima NOT running (re-verified 17:40; `colima start` only for nvcc-lane work, stop after) · 0 TinyGPU servers ·
DEXT `activated enabled` · 3090 enumerated (USB4) · swap 0.
**Rules in force (updated ~20:45): the blanket `DEV=NV*` ban is LIFTED — Artur granted autonomous use.**
Standing protocol replaces it: only on trees carrying the full remediation (master `3220d6e3d`+ / the
`task/T4.40c-halt-verify` branch); tiny-first escalation; exactly ONE server before any run; never
pkill under a live fault; no NV-opening pytest concurrent with dock work; serial pytest only; **gates run `DEV=CPU`** (the only unit-test NV
probes, `test_llm_device_map.py:194,382`, are `Device.DEFAULT == "METAL"`-gated; `test/device/test_hcq.py:620,635` opens
`Device["NV"]` unconditionally — exclude it); BEAM on
qwen3.6-class fresh searches only inside a T4.34 investigation.

## 7. Agent-ops lessons (cost real time; bake into every brief)
1. **Agents park mid-run** when the harness backgrounds >600 s commands (6 occurrences). Give them the
   blocking idiom IN the brief: `cmd > LOG 2>&1 & P=$!; until ! kill -0 $P 2>/dev/null; do sleep 20; done; tail LOG`.
   (Still works 2026-08-26; the current harness also has `run_in_background: true`, which re-invokes the agent when
   the command exits — the exact failure mode here.)
2. **Per-agent worktrees off a COMMIT** (`git worktree add … <sha> --detach` + `git switch -c`); branch
   names already checked out elsewhere error.
3. **"Pre-existing failure" claims must be verified against a commit OUTSIDE the day's work** — stashing
   your own diff proved nothing twice.
4. **cd-persistence**: a leading `cd <worktree>` in a bash call makes later heredoc docs-edits hit the
   wrong repo (happened twice) — use `git -C` / absolute paths for the memory branch.
5. **Serial pytest still spawns one real TinyGPU server per invocation** on this box; `-n12` spawns 12.
6. Verify agents' analytical headlines against known project data before they reach the docs (the 71 GB
   claim, "pre-existing failures", the fragmentation theory — all fell to one cheap measurement each).
7. **`pgrep -f` tripwires self-match**: your own backgrounded script's text (or another concurrent agent's)
   containing the pattern counts as a hit — verify matched PIDs against the real binary path
   (`ps -o command= -p PID | grep /Applications/TinyGPU.app`) before treating it as a real server
   (two agents hit this on 2026-08-26; neither acted on the false positive).
