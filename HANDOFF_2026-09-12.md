# HANDOFF 2026-09-12 — the card path is done: every format on hand-written NV kernels, one shared JIT arena, five Q4 files measured

Entry point for the next session. Earlier history: `HANDOFF_2026-09-10.md` (the card-path plan and the 09-11 measurements in its §3b/§3c),
`HANDOFF_2026-09-01.md` (fault era), `TASKS.md` (every task + the dated Status log — the 2026-09-11/12 rows are the evidence for everything here).

## 1. What runs, and how fast

**Standing server (plist `com.artur.pooled-serve`, since 09-11 11:39):** Qwen3.8-27B **Q4_0**, whole model on the RTX 3090, ctx 131072, 4-bit
KV, every custom kernel, state cache + vision encoder on the M3. **Candidate Artur is evaluating (left running 09-12 02:09):** **UD-Q4_K_XL at
212992** with `JIT_SHARED_ARENA=1` — `MODEL=/Users/artur/models/qwen3.8-27b-q4/Qwen3.8-27B-UD-Q4_K_XL.gguf CTX=212992 EXTRA_ENV="JIT_SHARED_ARENA=1"
SKIP_IDLE=1 ~/Documents/tinygrad-bench-data/deploy.sh`. The login LaunchAgent restores whatever the plist says (Q4_0 @131k until changed).

Measured (benchmark_llm, 2048-token prompt, 32 decode; flips = top-1 disagreement vs a Q8 reference on 24 synthetic prompts × 48 tokens):

| file on the card | prefill tok/s | decode tok/s | flips | 4-bit window: probe / **served** |
|---|---|---|---|---|
| Q4_0 (16.1 GB) | **119** | **23.2** | 2.34 % | 262k / 131k standing (~230k with the shared arena, untested) |
| UD-Q4_K_M (16.5) | 101 | 21.8 | 2.08 % | 246k / — |
| UD-Q4_K_S (15.4) | 98 | 21.2 | **1.91 %** | 262k / — |
| UD-Q4_K_XL (17.6) | 86 | 16–20 | 2.00 % | 213k / **213k with the shared arena** (< 98k without) |
| UD-IQ4_XS (14.2) | 82 | 19.5 | 2.95 % | 262k / — |
| Q8_0 pooled (the 09-06 map) | 19 | 5.2 | reference | — |

On the wire (2k-token prompt): Q4_0 ~100+ prefill / 24 gen; XL @213k 95 / 15–20. Window size does not change token speed (chain 21).
8-bit KV: +0.3 pt fewer flips, ~46 KB/token instead of ~30 on the card; ceilings in HANDOFF_2026-09-10 §3c.

## 2. What the memory is (per token and fixed) — the numbers that decide any window
- KV cache 18 KB/token (int4) / 35 (int8); plus ~12 KB/token of JIT-planned score arena per prefill family (2 slots × H=24 × chunk 32 × fp32).
- Fixed: weights (file size) + GDN states (~0.15 GB) + the vision encoder on METAL (not the card).
- **JIT families: three (greedy text, sampled text, image prompt), ~1.5 GB each at 131k — 4.6 GB — unless `JIT_SHARED_ARENA=1`** (T4.105,
  validated bit-identical): then one arena ≈ 1.6 GB, and greedy/sampled share one family anyway (temperature is decided inside the graph).
- Usable card memory ≈ 23.2 GB (every OOM lands there). A serving setting = weights + fixed + families + ctx × per-token ≤ 23.2.

## 3. The kernels (all on integration/t6, PR #44)
- `NV_CUSTOM_QUANT=1`: dp4a decode gemv (tokens ≤ 4) + `NV_WMMA` tensor-core prefill gemm, both for Q4_0/Q4_1/Q8_0/Q4_K/Q5_K/Q6_K/IQ4_XS/IQ4_NL/
  Q3_K/IQ3_XXS/IQ3_S (IQ2/Q2_K remnants stay generic). Fragment identity from the hardware warp lane (the lowerer splits a 32-wide LOCAL range
  by its div/mod usage; `test_llm_nv_gemm.py` pins the (4, 8) render); the gemm always reads a byte view of the packed weight.
- `GDN_NV_FUSED_DECODE=1`: the fused GDN scan serves decode. `nv_dense.py`: fp16 gemv for the small dense heads (copies made at load).
- `KQUANT_STAGE=0` (required on the card), `KV_INT4=1`, `STATE_CACHE_DEVICE=METAL`, `VISION_DEVICE=METAL`, `JIT_SHARED_ARENA=1` (recommended),
  `SDPA_HEAD_GROUPS` (0 = per-KV-head groups: score arena ÷ 4 at ~9 % prefill; off by default), T4.102's unroll budget in the optimizer.
- Tooling (durable copies in `~/Documents/tinygrad-bench-data/scripts/`, README there): deploy.sh, the device chains, ctx/serve-memory probes,
  the real-tensor validators, the WMMA probe + simulator; `prompts24.jsonl` + dumps beside them.

## 4. Rules learned this round (all in the repo CLAUDE.md / memory)
- Memory discipline: one verification wave at a time; agents test only their files serially, never `-n`, never pylint/mypy; **stop the server before
  any test wave** (its process holds pageable METAL weights on the pooled map) and run pytest one file per process. Two events forced this.
- A reboot wipes `/private/tmp/claude-501` — keep tooling and dumps in the repo or `~/Documents`; write results into TASKS.md as they land.
- The Claude Code shell exports the server knobs (KV_INT8, DEFAULT_TEMPERATURE, …): tests need `env -u …`; a chain's BASE env leaks into every
  step (the KV_INT4 flip scare) — give each measurement its own explicit env.
- pgrep-based idle checks must not match launch wrappers' argv (write scripts in one call, launch bare `zsh script.sh` in another).
- After every PR merge, merge fork master into integration/t6 (master's merge commits are not the branch's own).
- Workflow-file changes need the `workflow` OAuth scope (Artur pushes them); the fork's repo line cap is 29000 (T4.98 kernels counted).

## 5. Open board
- Artur: merge PR #44 (green, includes T4.105); choose the standing recipe (Q4_0 @131k / XL @213k / Q4_K_S — the quality pick); rebuild the
  real-prompt flip set if the real number matters (session-store extraction is PII-blocked for Claude).
- T4.103b tiled prefill attention (blocked by the static JIT graph); T4.94 MTP; IQ2/Q2_K formats; Hermes timeouts retuned for 100+/23 tok/s.
