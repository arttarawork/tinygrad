# HANDOFF 2026-09-10 — the card path (read this first; HANDOFF_2026-09-01 = the fault-era/WY history)

State at hand-off (night of 09-10; Artur: "Hermes isn't planned to be used for anything in the near future. Don't hesitate to stop it"):
the pooled server is DOWN on purpose, the 3090 is an experiment bench, and the standing recipe is unchanged for when it comes back
(`~/bin/pooled-serve.sh start` via the plist, or scratchpad `t4x/deploy.sh`; Q8_0 pooled map, LOOP_NUDGES=0, DEFAULT_TEMPERATURE_THINK=1.0).

## 1. What landed since HANDOFF_2026-09-01 (all on fork master 43ac66739 == integration/t6 up to a0714cf5a; PRs #39-#43)
- Serving: STREAM_LOG (T4.83), DEFAULT_TEMPERATURE + MIN_P (T4.85), THINK_BUDGET (T4.88), loop breaker (T4.89; now LOOP_NUDGES=0 = close the
  think block on the first repeated sentence), stream heartbeat (T4.90), reasoning_effort passthrough (T4.91 — medium injects NO template
  instruction, xhigh was the silent default), mode-split temperature + opt-in presence penalty (T4.92), tool_call promotion only for an
  UNCLOSED think block (T4.97 v2), image requests skip the state cache (T5.7c).
- Cache correctness: the splice cache had fed the model its previous turn TWICE since 08-27 (T4.95); the state cache never resumed a
  thinking session — two lingering references + tool-turn eviction (T4.96, now two-tier: boundary snapshots pinned). Unverified live: the
  first Hermes user turn after a restart should log `in:<boundary>` instead of `in:0`.
- Host snapshots: `STATE_CACHE_DEVICE=CPU` (T4.99) parks snapshots in Mac RAM (for the all-on-card layout; the pooled map's METAL share is
  host RAM already — keep STATE_CACHE_MB=2048 there).
- Hermes: compaction cap `compression.threshold_tokens: 65536` (Hermes floors models <512K ctx to a 75% trigger; the cap makes it 64k =
  the snapshot limit). Squelch (Qwen's synth) published as an artifact.
- Tools: `extra/benchmark_llm.py` (T0.3), `extra/launch_floor.py`, `extra/flip_rate.py dump|compare` (teacher-forced top-1 flip rate;
  slow: un-jitted forward, ~7 min per prompt of 48 tokens on the 27B), scratchpad `t4x/parse_profile_dev.py` for PROFILE=1 pickles
  (`/var/folders/.../T/profile.pkl.artur`). DEBUG=2 per-kernel times include queueing — never use them for budgets; use PROFILE=1.

## 2. The measurements that set the direction (all on the standing tree, verified BEAM winners)
| config | prefill | decode | note |
|---|---|---|---|
| Q8_0 pooled map (standing) | 18.3 tok/s | 4.34 | per token 217 ms: METAL 162 ms (82%, 38 DeltaNet blocks, ~0.15 ms per tiny kernel + ~100 GB/s), NV 36 ms (18%) |
| UD-Q4_K_XL on the 3090 (63 blocks) | 15.6 | 5.42 | needs KQUANT_STAGE=0 (the T4.2 scale staging = 0.25 B/weight captured in the jitted step: the 4.9 GiB OOM) |
| Q4_0 on the 3090 (all 64 blocks) | 16.2 | 6.92 | per token 172 ms: 1,700 launches (~18 ms of gaps at 11 µs each), weight gemvs 102 ms at 75-221 GB/s, GDN state kernels ~30 ms, tiny ssm_alpha/beta gemvs 24 ms, attention 6, LM head 7.5 |
Conclusions: the tinygrad-generated gemv kernels cap every quant near 100-200 GB/s on sm_86 (a 44 MB gemv takes 585 µs; bytes need 47 µs);
the pooled map is capped by the M3 (a faster NV gemv buys ≤12% there); the ONLY route to a new speed class is the whole model on the
card (Q4) with hand-written kernels: gemv → ~9 tok/s; + fused GDN decode + dense-head gemv + fewer launches → ~20 tok/s. Price: Q4 quality
(T4.93b flip-rate measurement in progress; the Q8 pooled reference dump exists: scratchpad t4x/q8_ref.npz, 24 real Hermes prompts).
Context on the card: 34 KB/token int8 KV → the XL fits 128k without vision, ~155k max; 4-bit KV (not built) would reach 256k.
Vision: `VISION_DEVICE=METAL` keeps Qwen's own encoder on the M3 (one small copy per image) — do NOT replace it with a captioning model.

## 3. T4.98 — hand-written NV quantized kernels (the AMD file is the template; nothing upstream to cherry-pick, merge-base 4bdc86513)
- a ✅ launch floor 12 µs/launch (dispatch overhead, not the dock). b ✅ upstream scan (notes/upstream_nv_scan_2026-09-10.md on
  task/T4.98-upstream-scan). c ✅ code: `kernels/nv_quant.py` — q8-activation dp4a decode gemv, Q4_K/Q5_K/Q6_K/IQ4_XS ported + Q8_0/Q4_0
  new, `NV_CUSTOM_QUANT=1` gate, decode-only (tokens ≤ 4). d 📋 device validation (scratchpad `t4x/nv_quant_validate.sh` → numerics vs the
  fused path + GB/s per format, then `nv_custom_model_bench.sh` → model decode on both layouts). e ✅ tools. f 📋 gemm for prefill (sm_80
  mma.sync fragment layout ≠ RDNA3 wmma). g 🔄 fused GDN decode (agent). h 🔄 f16 gemv for the tiny dense heads (agent).
- Upstream `5c6af9cb1` (WARP local dim) CONFLICTS in codegen/gpudims.py; `a0a901c8e` (faster qwen 3.8) conflicts with the new dispatch —
  both deferred, not required.

### 3b. 2026-09-11 — measured (benchmark_llm, 1024-token prompt, 32 decode, ctx 8192, cache-only BEAM)
| map | env | prefill | decode |
|---|---|---|---|
| Q4_0 card (0-63:NV, KQUANT_STAGE=0, KV_INT8) | baseline | 15.95 | 6.71 |
| Q4_0 card | NV_CUSTOM_QUANT=1 | 15.96 | 19.31 |
| Q4_0 card | + GDN_NV_FUSED_DECODE=1 | 15.94 | **22.97** |
| Q4_0 card | + JIT_BATCH_SIZE=4096 | 15.95 | 22.83 (no gain) |
| Q4_0 card | + KV_INT4=1 instead of KV_INT8 | 15.95 | 22.80 (no cost) |
| Q4_0 card | + NV_WMMA (tensor-core prefill gemm, T4.98f; 2048-token prompt) | **56.3** | 23.2 |
| Q4_0 card | + T4.98j (its Q4_1 ffn_down tensors on the tensor cores too; 09-11 12:32) | **119.4** | 23.2 |
| UD-Q4_K_XL card | decode gemv + fused GDN decode (no WMMA for K-quants); 2048-token prompt | 15.1 | 20.4 |
| UD-Q4_K_XL card | + T4.98j K-quant WMMA prefill (09-11 12:08; 2048-token prompt, 16 decode tokens) | **85.6** | 16.1 |
| UD-Q4_K_M card | all kernels incl. T4.98k (09-11 16:33) | 100.6 | 21.8 |
| UD-Q4_K_S card | all kernels incl. T4.98k | 97.5 | 21.2 |
| UD-IQ4_XS card | all kernels incl. T4.98k | 81.7 | 19.5 |
| Q8 pooled (standing map until 09-11 11:39) | baseline | 18.89 | 5.16 |
| Q8 pooled | NV_CUSTOM_QUANT=1 | 19.07 | 5.53 |
| Q8 pooled | + GDN_NV_FUSED_DECODE=1 | 18.93 | 5.35 |
| Q8 pooled | + NV_WMMA (2048-token prompt) | 26.9 | 5.8 |

Same 32 output ids in every run. Custom formats validated on real tensors: Q4_0, Q4_1, Q4_K, Q5_K, Q6_K, IQ4_XS, IQ4_NL, Q8_0 (rel err 2e-3–9e-3 from the int8 activations, top-1 agrees). The WMMA prefill gemm (T4.98f) was scrambled by the lowerer's local-range split (warp lane ≠ `lane`); fixed 09-11 and merged — probe exact, real tensors rel 2e-4. Quality, all five Q4-class files on the card with every kernel (24 synthetic prompts × 48 tokens vs a Q8 pooled reference, chains 4/8/11/15): Q4_0 2.34 %, UD-Q4_K_XL 2.00 %, UD-Q4_K_M 2.08 %, UD-Q4_K_S 1.91 %, UD-IQ4_XS 2.95 %. Earlier detail: Q4_0 card + every kernel incl. the WMMA prefill 2.34 % top-1 flips (decode kernels only 2.26 %), + KV_INT4 2.52 %, Q4_0 generic 2.34 % — the kernels cost nothing, KV_INT4 ~0.3 pt. Prompt set + dumps: `~/Documents/tinygrad-bench-data/`. **STANDING since 2026-09-11 11:39: the Q4_0 card map** (ctx 131072, KV_INT4 — KV_INT8 OOMs at 131k: 22.2 GB used —, state cache and vision encoder on METAL; plist rewritten, backup `.bak-20260911-1145-q8pooled`; bench 119 prefill / 23 decode after T4.98j; on the wire ~24 gen tok/s). XL on the card: 85.6 prefill (T4.98j) / 16–20 decode, 2.00 % flips. To serve it by hand: `MODEL=/Users/artur/models/qwen3.8-27b-q4/Qwen3.8-27B-Q4_0.gguf DMAP=0-63:NV CTX=131072 EXTRA_ENV="KQUANT_STAGE=0 NV_CUSTOM_QUANT=1 GDN_NV_FUSED_DECODE=1" SKIP_IDLE=1 t4x/deploy.sh` (NV_WMMA is on by default inside NV_CUSTOM_QUANT; quality: 5.3 % top-1 flips vs Q8 on the 09-10 real-prompt set, 2.3 % on the synthetic set — the kernels add nothing to either).

### 3c. Context ceilings on the card (09-11, t4x/ctx_probe.py; fits / first OOM)
| file | int4 KV | int8 KV | server-safe (≈ −48k / −32k) |
|---|---|---|---|
| Q4_0 (16.1 GB) | 262k fits | 164k / 180k | 213k / 115k |
| UD-Q4_K_XL (17.6) | 213k / 229k | 131k / 147k | 164k / 98k |
| UD-Q4_K_M (16.5) | 246k / 262k | 164k / 180k (OOM after T4.102's optimizer fix; before it a launch-resource error) | 197k / 131k |
| UD-Q4_K_S (15.4) | 262k fits | 180k / 197k | 213k+ / 147k |
| UD-IQ4_XS (14.2) | 262k fits | 197k / 213k | 246k / 164k |

Per token on the card: ~30 KB at int4, ~46 KB at int8 (the KV cache itself is 18 / 35 — ~12 KB/token of other context-proportional memory is unexplained). The serving process needs ~1.2–1.5 GB more than the probe.

## 4. Rules that still hold + new gotchas
- Never kill -9 the serve process or any `TinyGPU … server`; one DEV=NV process at a time (benchmarks, validation and the server are
  mutually exclusive — chain them, never overlap); stop/start only via `~/bin/pooled-serve.sh`; agents are CPU/NULL only and never push.
- Run `DEV=NULL test/null/test_llm_server*.py` with every serve.py change — CI runs them and nobody had locally (two PR failures on 09-09).
- Stacked PRs: merge top-down after retargeting to master, or delete each base branch right after merging it (09-10: #40-#42 merged into
  their bases; #43 fixed it).
- BEAM: cache-only for serving; a fresh search only by hand in an idle window (the Q4_K_XL leg took 5.4 h, Q4_0 43 min); winners are in
  `~/Library/Caches/tinygrad/cache.db` and persist. Custom kernels bypass BEAM entirely.
- Worktrees: t6int (serving tree), t498* / t499 (agents; delete after merge), t493 (KQUANT_STAGE, merged). Models: `~/models/qwen3.8-27b-q8`
  (standing), `~/models/qwen3.8-27b-q4` (UD-Q4_K_XL + Q4_0 + mmproj symlink).

- **2026-09-11 gotchas:** (a) the 00:34 kernel panic was RAM, not the dock — two agents' verification waves (`pytest -n12` + `pylint --jobs=0`
  + mypy) overlapped on the 36 GB Mac (colima VM ~7.5 GB): one wave at a time, agents test only their own files serially, lint is the
  orchestrator's with `--jobs=2` (CLAUDE.md conventions). (b) A reboot wipes `/private/tmp/claude-501` = the whole session scratchpad
  (scripts, logs, flip-rate dumps, profile pickles): keep anything worth keeping in the repo or `~/Documents`, write results into
  TASKS.md immediately. (c) The Claude Code shell can carry the standing server knobs (`KV_INT8=1 DEFAULT_TEMPERATURE=0.6
  THINK_BUDGET=16384 …`): 9 unit + 1 NULL test fail under them on any tree — run tests with `env -u KV_INT8 -u DEFAULT_TEMPERATURE …`.
  (d) `NV_CUSTOM_QUANT=1` had never run on a real model until 09-11: `set_quantized` assumed a SHRINK that tensors staged alone
  (>64 MB) don't have (fixed, e7735016b). Formats per file (header census): Q4_0 file = 12.4 GB Q4_0 + 1.0 Q6_K + 1.0 Q5_K + 0.4 Q4_1
  (uncovered) ; XL = Q5_K 7.4 + IQ4_XS 2.9 + Q4_K 2.9 + Q6_K 2.7 + IQ4_NL 0.26 / Q3_K 0.11 / IQ3_S 0.04 (uncovered); Q8 = all Q8_0.

## 5. Open board (see TASKS.md for the full entries)
T4.98d/f/g/h (above) → T4.93b quality gate → T4.94 MTP on the card build → T4.96 live check → 4-bit KV for 256k (unwritten) → T3.6, T4.8 parked.
